apply plugin: 'java'

buildscript{
    repositories {
        mavenCentral()
    }
}

apply plugin: 'maven'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'org.greenrobot:greendao-generator:3.2.0'
}